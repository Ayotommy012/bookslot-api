# bookslot-api
A booking system API where users reserve slots on resources (rooms, classes, services) and admins manage availability. Built to demonstrate real backend concerns — transactional capacity checks to prevent overbooking, role-based middleware, and validation beyond basic CRUD — not just another Express boilerplate.
